# V1.4.7B Mattermost + Codex 多 Agent 协同底座

## Summary

本轮不做业务功能，只把 `OpenSpec + GitNexus + Mattermost + GitHub + Codex` 固化为多人协同链路。

## Key Changes

- 新建 OpenSpec change：`multi-codex-coordination-bus`。
- 新增 repo-scoped skill：`.agents/skills/codex-coordination/`。
- 新增 Mattermost runbook、channel protocol、multi-Codex protocol、MCP config example。
- 新增 REST fallback posting scripts。
- 更新 `AGENTS.md`、README、快速上手、OpenSpec/Codex runbook、PR review runbook、collaboration guide。
- `.gitignore` 增加 Mattermost local secret patterns。

## Verification

- `openspec validate multi-codex-coordination-bus --strict`
- `openspec validate --all`
- `git diff --check`
- `curl http://8.141.111.33:8065`
- Mattermost channel / PAT / MCP / PLAN-ACK-CHANGED-REVIEW_READY smoke

## Current Blocker

`http://8.141.111.33:8065` 当前超时。需要 #1 在 VPS 上部署 Mattermost 或开放 `8065` 后继续真实联调。

