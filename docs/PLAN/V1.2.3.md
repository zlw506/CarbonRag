# CarbonRag V1.2.3 OpenSpec 格式修复与根 Agent 指令

## Summary

本轮只修 OpenSpec / governance 文件格式和根目录 agent 指令，不改业务功能、不改 UI、不碰 AI 聊天主链路。

## Changes

- 校验并保留 `openspec/config.yaml` 为合法 YAML。
- 校验 `.gitignore`、CODEOWNERS、PR 模板、OpenSpec specs、Codex skills 的多行结构。
- 新增根目录 `AGENTS.md`，要求实现前先读 `openspec/specs/**`，非平凡功能必须有 OpenSpec change-id。
- 明确 `.spec-gen/` 和 `3rdparty/spec-gen/` 是本地试点产物，不得提交。
- 修复 README 中仍指向旧嵌套目录的文档链接。

## Verification

- `openspec list`
- `openspec validate --all`
- `git check-ignore -v .spec-gen/analysis/test.txt`
