# V1.4.6 #1 融合测试开发日志

## 本轮目标

将 #1 已完成的 V1.4.5 碳核算可信底座，与 #2 提交并完成修复的 V1.3.X RAG initial baseline 在本地 `main` 进行融合测试。确认两条主线可以共存后，再推送 `main` 作为云端发布候选。

## 实际完成内容

- 在本地 `main` 合并 #2 PR #3：`V1.3.X #2 RAG initial baseline`。
- 保留 V1.4.5 碳核算可信底座，包括 `carbon-inventory-trust-baseline`、可信持久化表、官方/缺省因子种子和测试。
- 合入 #2 RAG 初版系统包，包括 RAG Lab、retrieval-only API、统一 RAG schema、BM25/vector hybrid 边界、pgvector adapter 预留、parser provider 预留、rerank/eval baseline、graph experiment 边界和 RAG workflow governance。
- 确认 #2 已修复上一轮 #1 驳回项：private/mixed 空选择 fail-closed、安全错误响应、retrieval-only 只读、版本说明统一为 V1.3.X。
- 更新根目录 `开发公告.md`，公布 V1.4.6 融合测试结论。

## 验证结果

- `openspec validate --all`：25 passed, 0 failed。
- `backend/.conda/python.exe -m pytest -q`：206 passed, 4 warnings。
- `frontend npm run typecheck`：通过。
- `frontend npm run build`：通过，仅 Vite chunk size warning。
- `git diff --check HEAD~1..HEAD`：通过。

## 已知限制

- RAG Lab 属于 retrieval-only 实验入口，不等同于主 ask 链路完全切换到新 RAG engine。
- Docling、MinerU、pgvector、graph query modes 仍为可选或实验能力，默认不应成为本地/云端强依赖。
- #2 开发日志目录仍为 `日志/#2/V1.3.1/`，正文已明确该 PR 是 V1.3.X baseline；后续 #2 必须统一目录名、标题、PR body 和实际范围。
- 前端 build 仍有 `ui_vendor` chunk size warning，非本轮阻断项。

## 后续交接事项

- 云端发布后需要至少 smoke：登录、Ask、RAG Lab、碳核算、报告入口、管理员基本页。
- 若云端出现 RAG Lab 或 optional provider 相关异常，优先检查 `.env.example` 新增 RAG/embedding 配置与生产环境变量是否一致。
- #1 后续继续 V1.4.X 碳核算主线；#2 后续继续 V1.3.X / 下一阶段 RAG 主线，但必须遵守本轮版本纪律。
