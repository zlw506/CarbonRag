# V1.3.1 #2 RAG 主线阶段性开发日志

## 本轮目标

按最新 `Git-ys1/CarbonRag` 开发公告要求，将 #2 的 V1.3.x RAG / LightRAG-inspired 主线整理为阶段性 PR，并补齐发布前开发日志。

## 实际完成内容

- 建立并实现 RAG Lab 可观测性增强：后端地址、请求参数、错误详情、0 命中提示、fallback metadata 展示。
- 新增统一 RAG 数据契约：`ParsedDocument`、`DocumentBlock`、`ChunkRecord`、`EmbeddingRecord`、`CitationRef`、`RetrievalTrace`。
- 新增 ParserProvider、VectorStoreAdapter、HybridRetriever、RerankProvider、GraphIndexBuilder、Graph query modes 等 RAG 实验边界。
- 新增可选 Docling / MinerU / pgvector adapter 预留，默认关闭，不作为本地强依赖。
- 新增 RAG 检索评测集和 `scripts/rag_eval.py` 基线。
- 新增轻量 RAG ingest workflow 与治理字段预留，记录上传、解析、切块、索引、图谱候选等节点状态。
- 合入 upstream/main 最新 V1.4.5 开发公告和碳核算可信底座，按公告补本开发日志。

## 验证结果

- `openspec validate rag-workflow-governance-foundation --strict` 通过。
- `openspec validate --all` 已在合入 upstream 前通过；合入 upstream 后将随 PR 前检查再次验证。
- 后端全量测试已在合入 upstream 前通过：`177 passed`。
- 前端 `npm run typecheck` 通过。
- 前端 `npm run build` 通过，仅保留 Vite chunk size warning。

## 已知限制

- 本轮按 V1.3.1 阶段性 PR 发布口径提交给 #1 审查；代码内保留若干后续 RAG 能力边界的本地演进 change-id，需由 #1 决定是否一次性合入或拆分。
- Docling、MinerU、pgvector、Graph query modes 均为可选或实验能力，默认不改变 `/ask` 行为。
- GraphRAG、Neo4j、LangGraph、Keycloak/OpenFGA、K8s 等企业重依赖未接入。

## 后续交接事项

- #1 重点审查 M5 Knowledge / File / RAG 的边界、默认行为兼容性、OpenSpec change 数量是否需要拆分。
- 如 #1 要求缩小 PR，可优先保留 RAG Lab、统一 schema、adapter boundary、hybrid retrieval 四类 V1.3.x 最小能力。
- 合并前请继续确认 `/ask`、session、report、calc_carbon 回归测试结果。
