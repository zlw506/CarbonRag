import { PageScaffold } from "../../components/PageScaffold";

export function ReportPage() {
    return (
        <PageScaffold
            title="报告生成页占位"
            description="用于固定未来报告参数与输出区域。当前不实现真实报告生成。"
            formTitle="报告配置区"
            resultTitle="报告输出区"
        />
    );
}
